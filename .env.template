# Brick Check Pipeline Environment Configuration
# Copy this file to .env and configure your specific values

# GitHub Personal Access Token (REQUIRED)
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo (for private repos), public_repo (for public repos)
GITHUB_TOKEN=your_github_token_here

# Optional: DVC Remote Storage Configuration
# Uncomment and configure if you want to use remote storage for DVC cache
# DVC_REMOTE_URL=s3://your-bucket/path
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# Optional: Google Cloud Storage for DVC
# GCP_PROJECT_ID=your-gcp-project
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Optional: Notification Settings
# Slack webhook for notifications (uncomment to enable)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/your/webhook/url

# Optional: Email notifications (requires sendmail or SMTP setup)
# NOTIFICATION_EMAIL=admin@yourcompany.com

# Optional: Pipeline Configuration
# Maximum number of parallel brick installations (default: 4)
# MAX_PARALLEL_JOBS=4

# Optional: Resource Limits
# Maximum memory per container (default: 4G)
# MAX_MEMORY=4G

# Optional: Log Level (debug, info, warning, error)
LOG_LEVEL=info

# Optional: Custom GitHub API endpoint (for GitHub Enterprise)
# GITHUB_API_URL=https://api.github.com

# Optional: DVC Configuration
# Force DVC to always fetch latest data
# DVC_FORCE_REPRO=false

# Google Cloud Configuration (REQUIRED for GCloud Console logging)
# Your Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Google Cloud Service Account Key (optional - uses VM metadata service if not set)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
